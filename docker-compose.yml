services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: seclog
      POSTGRES_PASSWORD: seclog_dev
      POSTGRES_DB: seclog
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
